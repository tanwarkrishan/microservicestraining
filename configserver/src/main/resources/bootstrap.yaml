spring:
  application:
    name: config-server
      cloud:    config:      server:        git:          uri: C:\labs\gitrepo
          clone-on-start: true
    
    
server:  port: 8110
  
management:
  endpoints:
    web:
      exposure:
        include: "*"