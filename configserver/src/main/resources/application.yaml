eureka:
  instance:
    instance-id: ${spring.application.name}:${random.int}
    
  client:
    service-url:
      defaultZone : http://localhost:8090/eureka
      
spring:  rabbitmq:    host: 192.168.99.100
    port: 5672
    username: guest
    password: guest
    